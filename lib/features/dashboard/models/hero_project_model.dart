class HeroProjectModel {
  final String category;
  final String title;
  final String description;
  final String buttonText;

  const HeroProjectModel({
    required this.category,
    required this.title,
    required this.description,
    required this.buttonText,
  });
}